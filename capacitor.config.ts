import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'foodie.app.starter',
  appName: 'foodieapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
